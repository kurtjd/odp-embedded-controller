[package]
edition = "2021"
license = "MIT"
publish = false
name = "dev-qemu"
version = "0.1.0"

[[bin]]
bench = false
name = "dev-qemu"
test = false

[profile.release]
lto = true          # better optimizations
codegen-units = 1
debug = true
opt-level = "z"
panic = "abort"
incremental = false

# Optimize dependencies for size but keep root crate unoptimized for better debuggability
[profile.dev.package."*"]
opt-level = "z"

# Uncomment to turn off optimization for specific dependency (such as when debugging a service)
# [profile.dev.package.dependency-name]
# opt-level = 0

[lints.rust]
warnings = "deny"

[lints.clippy]
correctness = "deny"
perf = "deny"
suspicious = "deny"
style = "deny"

# The `embassy_executor::main` macro expands to code referencing `riscv_rt`,
# but cargo-machete cannot see through the macro. Keep the direct dependency
# and tell machete to ignore it.
[package.metadata.cargo-machete]
ignored = ["riscv-rt"]

[dependencies]
riscv-rt = "0.17.0"

embassy-qemu-riscv = { git = "https://github.com/kurtjd/qemu-riscv-rs", features = [
    "defmt",
] }
embedded-mcu-hal = { git = "https://github.com/OpenDevicePartnership/embedded-mcu", features = [
    "defmt",
] }
embassy-executor = { version = "0.10.0", features = [
    "platform-riscv32",
    "executor-thread",
    "defmt",
] }

# HID-over-I2C keyboard demo services driven by UART input. All three crates
# live in the embedded-services repo, so they must share the same git revision.
embedded-services = { git = "https://github.com/OpenDevicePartnership/embedded-services", branch = "main", features = [
    "defmt",
] }
hid-service = { git = "https://github.com/OpenDevicePartnership/embedded-services", branch = "main", features = [
    "defmt",
] }
keyboard-service = { git = "https://github.com/OpenDevicePartnership/embedded-services", branch = "main", features = [
    "defmt",
] }

embassy-sync = "0.8"
static_cell = "2.1.0"
defmt = "0.3.6"
defmt-semihosting = "0.3.0"
semihosting = { version = "0.1", features = ["panic-handler"] }
